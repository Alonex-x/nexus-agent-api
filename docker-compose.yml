version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: nexusdb
      POSTGRES_USER: nexususer
      POSTGRES_PASSWORD: nexuspass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
